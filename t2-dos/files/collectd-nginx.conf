# Configured on top of nearly empty /etc/collectd.conf

Interval 2

LoadPlugin nginx

<Plugin nginx>
  URL "http://127.0.0.1/status"
</Plugin>

LoadPlugin network

<Plugin network>
  Server "monitor" "25826"
</Plugin>

LoadPlugin tcpconns

<Plugin tcpconns>
  ListeningPorts true
</Plugin>
